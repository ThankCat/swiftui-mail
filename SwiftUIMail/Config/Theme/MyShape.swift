//
//  MyShape.swift
//  常用图形
//
//  Created by smile on 2024/9/10.
//

import SwiftUI

// 定义圆角矩形形状
let extraSmallRoundedCornerShape = RoundedRectangle(
    cornerRadius: extraSmallCornerRadius)

let extraSmallShape = RoundedRectangle(cornerRadius: extraSmallCornerRadius)
let smallShape = RoundedRectangle(cornerRadius: smallCornerRadius)
let mediumShape = RoundedRectangle(cornerRadius: mediumCornerRadius)
let largeShape = RoundedRectangle(cornerRadius: largeCornerRadius)
let extraLargeShape = RoundedRectangle(cornerRadius: extraLargeCornerRadius)
